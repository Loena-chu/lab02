# Block 4 Labs (Lecture 7–8)
## Files
- **lab7_decisions_branching.py**: if/elif/else, boolean logic, guard conditions
  - Includes boundary tests for temperature thresholds (-20, 10, 60)
  - Improved alarm rule readability with named boolean variables
  - Demonstrates temperature classification (-20 to 120°C range)

- **lab8_if_with_lists.py**: using lists in decisions (allowed/blocked, membership, simple rules)
  - Uses `in` operator for list membership checking
  - Command validation rule system (allowed/blocked lists)
  - Sensor reading classification with if/elif/else
  - Uses `any()` and `all()` for aggregate checks on readings
  - Improved beginner-friendly error messages


